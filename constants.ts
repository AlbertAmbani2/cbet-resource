
import { TVETCategory, TVETLevel } from './types';

export const CATEGORIES = Object.values(TVETCategory);
export const LEVELS = Object.values(TVETLevel);

export const THEME = {
  primary: 'blue-600',
  secondary: 'slate-600',
  accent: 'teal-500',
  background: 'slate-50',
};
